_schema-version: "3.2"
ID: EstadoCuentaDev-extension
extends: EstadoCuenta

parameters:
  appname: estadocuenta-dev
  subdomain: ${org}

modules:
  - name: estadocuenta-approuter
    parameters:
      memory: 256M
      disk-quota: 256M
      routes:
        - route: https://${subdomain}-${appname}.${default-domain}

resources:

  - name: uaa_EstadoCuenta
    parameters:
      service-name: ${appname}-xsuaa
      config:
        xsappname: ${appname}

  - name: EstadoCuenta_saas_registry
    parameters:
      service-name: ${appname}-saas-registry
      config:
        xsappname: ${appname}
        appName: ${appname}
        displayName: Estado de Cuenta Dev
        description: Aplicación Estado de Cuenta - Ambiente Dev

  - name: AppsPortalFront-destination-service
    parameters:
      service-name: AppsPortalFront-dev-destination

  - name: EstadoCuenta_html_repo_host
    parameters:
      service-name: EstadoCuenta-Dev-html5-host

  - name: AppsPortalFront_html_repo_runtime
    parameters:
      service-name: AppsPortalFront-dev-html5-runtime