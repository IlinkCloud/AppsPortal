_schema-version: "3.2"
ID: EnviarFacturaExtTest-extension
extends: EnviarFacturaExt

parameters:
  appname: enviarfacturaext-test
  subdomain: ${org}

modules:
  - name: enviarfacturaext-approuter
    parameters:
      memory: 256M
      disk-quota: 256M
      routes:
        - route: https://${subdomain}-${appname}.${default-domain}

resources:

  - name: uaa_EnviarFacturaExt
    parameters:
      service-name: ${appname}-xsuaa
      config:
        xsappname: ${appname}

  - name: EnviarFacturaExt_saas_registry
    parameters:
      service-name: ${appname}-saas-registry
      config:
        xsappname: ${appname}
        appName: ${appname}
        displayName: Factura Extranjera Test
        description: Aplicación Factura Extranjera - Ambiente Test

  - name: AppsPortalFront-destination-service
    parameters:
      service-name: AppsPortalFront-test-destination

  - name: EnviarFacturaExt_html_repo_host
    parameters:
      service-name: EnviarFacturaExt-Test-html5-host

  - name: AppsPortalFront_html_repo_runtime
    parameters:
      service-name: AppsPortalFront-test-html5-runtime