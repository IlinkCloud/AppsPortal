_schema-version: "3.2"
ID: EnviarFacturasDev-extension
extends: EnviarFacturas

parameters:
  appname: enviarfacturas-dev
  subdomain: ${org}

modules:
  - name: enviarfacturas-approuter
    parameters:
      memory: 256M
      disk-quota: 256M
      routes:
        - route: https://${subdomain}-${appname}.${default-domain}

resources:

  - name: uaa_EnviarFacturas
    parameters:
      service-name: ${appname}-xsuaa
      config:
        xsappname: ${appname}

  - name: EnviarFacturas_saas_registry
    parameters:
      service-name: ${appname}-saas-registry
      config:
        xsappname: ${appname}
        appName: ${appname}
        displayName: Enviar Facturas Dev
        description: Aplicacion Enviar Facturas - Ambiente Dev

  - name: AppsPortalFront-destination-service
    parameters:
      service-name: AppsPortalFront-dev-destination

  - name: EnviarFacturas_html_repo_host
    parameters:
      service-name: EnviarFacturas-Dev-html5-host

  - name: AppsPortalFront_html_repo_runtime
    parameters:
      service-name: AppsPortalFront-dev-html5-runtime